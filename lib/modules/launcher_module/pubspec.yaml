name: server_launcher
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.18+34

environment:
  sdk: ^3.9.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # State management
  flutter_bloc: ^8.1.6

  # Desktop window control and frameless support
  window_manager: ^0.4.2
  
  # Video background (media_kit for reliable desktop playback)
  media_kit: ^1.1.11
  media_kit_video: ^1.2.8
  media_kit_libs_windows_video: ^1.0.11

  # Open external links
  url_launcher: ^6.3.0

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  ftpconnect: ^2.0.3
  dartssh2: ^2.16.0
  # File picker for desktop/exe selection
  file_picker: ^10.3.3
  # Updater dependencies
  shared_preferences: ^2.1.0
  archive: ^3.3.4
  path: ^1.9.0
  # Package info
  package_info_plus: ^8.1.2
  # Crypto — PBKDF2-XOR decryption (shared with generator)
  crypto: ^3.0.3
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.4

flutter_launcher_icons:
  android: false
  ios: false
  image_path: "assets/images/logo.png"
  windows:
    generate: true
    image_path: "assets/images/logo.png"
    icon_size: 256

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/video/smok.mp4
    - assets/images/valheim.png
    - assets/manifest.sig
    - assets/config_encrypted.json
    - assets/panel_config.json
    - assets/manifest.sig
    - assets/doorstop.zip
    - assets/lang/en/translations.json
    - assets/lang/en/flag.png
    - assets/lang/pl/translations.json
    - assets/lang/pl/flag.png
    - assets/lang/ua/translations.json
    - assets/lang/ua/flag.png
    - assets/lang/de/translations.json
    - assets/lang/de/flag.png
    - assets/lang/fr/translations.json
    - assets/lang/fr/flag.png
    - assets/lang/es/translations.json
    - assets/lang/es/flag.png
    - assets/lang/bg/translations.json
    - assets/lang/bg/flag.png
    - assets/lang/hr/translations.json
    - assets/lang/hr/flag.png
    - assets/lang/cs/translations.json
    - assets/lang/cs/flag.png
    - assets/lang/da/translations.json
    - assets/lang/da/flag.png
    - assets/lang/nl/translations.json
    - assets/lang/nl/flag.png
    - assets/lang/et/translations.json
    - assets/lang/et/flag.png
    - assets/lang/fi/translations.json
    - assets/lang/fi/flag.png
    - assets/lang/el/translations.json
    - assets/lang/el/flag.png
    - assets/lang/hu/translations.json
    - assets/lang/hu/flag.png
    - assets/lang/ga/translations.json
    - assets/lang/ga/flag.png
    - assets/lang/it/translations.json
    - assets/lang/it/flag.png
    - assets/lang/lv/translations.json
    - assets/lang/lv/flag.png
    - assets/lang/lt/translations.json
    - assets/lang/lt/flag.png
    - assets/lang/mt/translations.json
    - assets/lang/mt/flag.png
    - assets/lang/pt/translations.json
    - assets/lang/pt/flag.png
    - assets/lang/ro/translations.json
    - assets/lang/ro/flag.png
    - assets/lang/sk/translations.json
    - assets/lang/sk/flag.png
    - assets/lang/sl/translations.json
    - assets/lang/sl/flag.png
    - assets/lang/sv/translations.json
    - assets/lang/sv/flag.png

  fonts:
    - family: Norse
      fonts:
        - asset: assets/fonts/Norse.otf
        - asset: assets/fonts/Norsebold.otf
          weight: 700

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
